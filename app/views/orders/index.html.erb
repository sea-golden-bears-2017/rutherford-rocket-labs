<div class="">
  <h1>Orders</h1>
  <table class="list">
    <tr>
      <th>Order #</th>
      <th>Date Created</th>
    </tr>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.id %></td>
        <td><%= order.created_at %></td>
      </tr>
    <% end %>
  </table>
</div>
